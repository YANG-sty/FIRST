<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gree.first.user.dao.UserMapper">

    <select id="queryByName" resultType="com.gree.first.user.domain.User">
        select * from first_user where user_name=#{name}
    </select>

</mapper>